{
  "$schema": "https://raw.githubusercontent.com/anthropics/claude-code/main/schemas/mcp.schema.json",
  "mcpServers": {
    "mind": {
      "command": "npx",
      "args": ["-y", "@anthropic-ai/mcp-mind"],
      "description": "Mind - Local persistent memory across sessions"
    },
    "spawner": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://mcp.vibeship.co/mcp"],
      "description": "VibeShip Spawner - Cloud skills, validation, and project orchestration"
    },
    "scanner": {
      "command": "npx",
      "args": ["-y", "mcp-remote", "https://scanner.vibeship.co/mcp"],
      "description": "VibeShip Scanner - Security scanning, CVE/CWE lookup, and vulnerability analysis"
    }
  }
}
